# Optional. Keep blank to use unauthenticated public GitHub requests.
# Use a dedicated read-only token with public-repository access only.
# Never use NEXT_PUBLIC_ for credentials. Never commit real tokens.
GITHUB_TOKEN=
